@inject NavigationManager NavigationManager
@rendermode InteractiveServer

<button @onclick="EditGame" class="btn btn-primary">EditGame</button>

@code {
    [Parameter]
    public int Id { get; set; }

    void EditGame()
    {
        NavigationManager.NavigateTo($"/editgame/{Id}");
    }
}
