@inject NavigationManager NavigationManager
@rendermode InteractiveServer

<button @onclick="CreateGame" class="btn btn-primary">AddGame</button>

@code {

    void CreateGame()
    {
        NavigationManager.NavigateTo("/editgame");
    }
}
